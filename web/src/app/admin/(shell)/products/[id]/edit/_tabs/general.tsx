/**
 * "General" tab placeholder. The real form (name, slug, SKU,
 * descriptions, base price, stock status, min/max order qty, featured
 * toggle) lands in P2-T11 alongside the per-tab auto-save model.
 */
export function GeneralTab() {
  return (
    <div className="rounded-lg border border-husk-200 bg-paper-0 p-6 text-sm text-stone-500">
      Editable fields land here in <span className="font-mono">P2-T11</span>.
    </div>
  );
}
